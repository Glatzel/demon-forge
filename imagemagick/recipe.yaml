package:
  name: imagemagick
  version: ${{ version }}
context:
  version: 7.1.1.44
source:
  git: https://github.com/ImageMagick/ImageMagick.git
build:
  number: 0
  script:
  - then:
    - pwsh -f %PREFIX%\build.ps1
    if: win
tests:
- script:
  - magick --help

