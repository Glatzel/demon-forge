source:
  git: https://github.com/ImageMagick/ImageMagick.git
tests:
- script:
  - magick --help
package:
  version: ${{ version }}
  name: imagemagick
context:
  version: 7.1.1.44
build:
  script:
  - if: win
    then:
    - pwsh -f %RECIPE_DIR%\build.ps1
  number: 0
