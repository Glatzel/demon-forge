source:
  git: https://github.com/jqlang/jq.git
build:
  script:
  - if: win
    then:
    - pwsh -f %RECIPE_DIR%\build.ps1
  number: 0
tests:
- script:
  - jq --help
package:
  version: ${{ version }}
  name: jq
context:
  version: 1.7.1

