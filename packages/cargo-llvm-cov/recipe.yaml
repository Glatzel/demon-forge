context:
  name: cargo-llvm-cov
package:
  version: 0.6.17
  name: ${{ name }}
build:
  number: 0
  script:
    - if: win
      then:
        - pwsh -f %RECIPE_DIR%\build-${{ name }}.ps1
about:
  repository: https://github.com/jqlang/${{ name }}
  description: |
    [exposed]

    ${{ name }} = "${{ name }}"
