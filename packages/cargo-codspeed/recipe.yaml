context:
  name: cargo-codspeed
package:
  version: "4.5.0"
  name: ${{ name }}
build:
  number: 0
requirements:
  build:
    - ${{ compiler('rust') }}
tests:
  - package_contents:
      bin:
        - ${{name}}
