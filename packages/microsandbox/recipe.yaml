context:
  name: microsandbox
  version: "0.3.1"
package:
  version: ${{ version }}
  name: ${{ name }}

build:
  number: 0
  script:
    interpreter: powershell
    file: build.ps1
requirements:
  build:
    - ${{ compiler("c") if linux }}
    - ${{ stdlib("c") if linux }}
  host:
    - if: linux
      then:
        - openssl
        - libgcc
        - libcap
tests:
  - package_contents:
      bin:
        - msb
        - msbnet
  - script:
      - msb --help
about:
  repository: https://github.com/zerocore-ai/microsandbox
