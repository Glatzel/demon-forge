context:
  name: houdini-launcher
package:
  version: "22.0"
  name: ${{ name }}
build:
  number: 1
requirements:
  build:
    - aria2
    - vinaya-cli
    - jq
tests:
  - script:
      - if: linux
        then: install_houdini_launcher houdini houdini_launcher

  - package_contents:
      files:
        - ${{"houdini-launcher/bin/houdini_launcher.exe" if win}}

extra:
  update: echo "22.0"
  changelog: echo "https://www.sidefx.com/changelog/"
