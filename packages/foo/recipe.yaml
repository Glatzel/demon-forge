context:
  name: cargo-bloat
package:
  version: "0.12.1"
  name: cargo-bloat
build:
  script:
    - env | sort
    - cargo install --root ${PREFIX} cargo-llvm-cov
requirements:
  build:
    - rust
tests:
  - package_contents:
      bin:
        - ${{ name }}
