context:
  name: nerdctl-full
package:
  version: "2.2.1"
  name: ${{ name }}
build:
  number: 0
  script:
    interpreter: powershell
    file: build.ps1
requirements:
  run:
    - runc
tests:
  - script: nerdctl --help
  - script: runc --help
